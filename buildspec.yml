version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn test
      - mvn package
  post_build:
    commands:
      - echo Build completed on `date`
      - ls ./target
artifacts:
  files:
    - target/spring-petclinic-3.0.0.BUILD-SNAPSHOT.jar

